# TaxDecod QA Checklist

## 1. Branch safety

Before editing:

- confirm branch is `taxdecod-v2-redesign`
- confirm working tree is clean
- do not edit `main` directly
- do not merge until approved

Commands:

```powershell
git branch --show-current
git status --short